added module partitions
added "linkage specifications opt out of module ownership"
added module preamble syntax
added 'module;' introducer (p0713r1, approved at jax)
header import semantics
module import declaration shall only appear within preamble if there is a module-declaration
availability rule (effects may be available despite the declaration not being visible)
header import preprocessor effects
preamble identification

WORDING QUESTIONS:
do we need ASG etc? can we simplify it?
do we need modules-as-entities?

TO DISCUSS WITH EWG:
namespace export rules
ADL and path of instantiation
#export
public import or restricted import
